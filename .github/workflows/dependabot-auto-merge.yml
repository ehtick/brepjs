name: Dependabot Auto-merge

on:
  pull_request:
    types: [opened, synchronize]

permissions: {}

jobs:
  auto-merge:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    if: github.actor == 'dependabot[bot]'
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: dependabot/fetch-metadata@25dd0e34f4fe68f24cc83900b1fe3fe149efef98 # v2
        id: metadata
      # Intentionally auto-approves patch/minor deps without human review.
      # --auto waits for CI to pass before merging. Major updates require manual review.
      - name: Auto-approve and merge patch/minor updates
        if: steps.metadata.outputs.update-type != 'version-update:semver-major'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_URL: ${{ github.event.pull_request.html_url }}
        run: |
          gh pr review "$PR_URL" --approve
          gh pr merge "$PR_URL" --auto --squash
