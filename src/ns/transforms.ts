/**
 * Namespace: transforms — spatial transformations
 */
export {
  translate,
  rotate,
  mirror,
  scale,
  clone,
  applyMatrix,
  composeTransforms,
  transformCopy,
} from '@/topology/api.js';
