/**
 * Namespace: modifiers — shape modification operations
 */
export { fillet, chamfer, shell, offset, thicken, draft, heal, simplify } from '@/topology/api.js';

export { variableFillet } from '@/topology/modifierFns.js';
